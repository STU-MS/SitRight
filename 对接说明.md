# 坐姿矫正仪 - 软硬件对接说明

## 一、系统架构流程图

```mermaid
graph TB
    subgraph 硬件端[Arduino 硬件]
        A[角度传感器读取] --> B[坐姿状态判断]
        B --> C[LED 三级提醒]
        C --> D[串口数据发送]
    end

    subgraph 通信[串口通信]
        E["STATE:2,ANGLE:780"]
    end

    subgraph 软件端[C# 软件]
        F[串口数据解析] --> G[屏幕模糊控制]
        G --> H[白屏/恢复正常]
    end

    subgraph 屏幕效果[屏幕效果]
        I[坐姿端正 → 屏幕正常]
        J[轻度不端正 → 屏幕轻度模糊]
        K[重度不端正 → 屏幕白屏]
    end

    硬件端 --> 通信 --> 软件端 --> 屏幕效果
```

---

## 二、硬件工作流程图

```mermaid
flowchart TD
    Start([开始]) --> Read[读取角度传感器 A0]
    Read --> Compare{判断角度值}
    
    Compare -->|< 300| State0[状态 0: 端正]
    Compare -->|300 ~ 600| State1[状态 1: 轻度不端正]
    Compare -->|> 600| State2[状态 2: 重度不端正]
    
    State0 --> LED0[绿灯亮]
    State1 --> LED1[蓝灯亮]
    State2 --> LED2[红灯亮]
    
    LED0 --> Send0[串口发送 STATE:0,ANGLE:xxx]
    LED1 --> Send1[串口发送 STATE:1,ANGLE:xxx]
    LED2 --> Send2[串口发送 STATE:2,ANGLE:xxx]
    
    Send0 --> Delay[延时 100ms]
    Send1 --> Delay
    Send2 --> Delay
    
    Delay --> Read
```

---

## 三、数据流简图

```mermaid
flowchart LR
    A[角度传感器] --> B[Arduino]
    B -->|串口| C[C# 程序]
    C --> D[屏幕模糊/白屏]
```

---

## 四、状态对应图

```mermaid
graph LR
    subgraph 硬件
        A0[角度值 < 300] --> S0[STATE:0]
        A1[角度值 300~600] --> S1[STATE:1]
        A2[角度值 > 600] --> S2[STATE:2]
    end
    
    subgraph 软件
        S0 --> SC0[屏幕正常]
        S1 --> SC1[屏幕轻度模糊]
        S2 --> SC2[屏幕白屏]
    end
    
    subgraph LED
        S0 --> L0[ 绿灯]
        S1 --> L1[ 蓝灯]
        S2 --> L2[ 红灯]
    end
```

---

## 五、串口通信参数

| 项目 | 值 |
|------|-----|
| 波特率 | 9600 |
| 数据位 | 8 |
| 停止位 | 1 |
| 校验位 | 无 |
| 通信接口 | USB 串口 (虚拟 COM 口) |

---

## 六、数据格式

硬件每 100ms 发送一条数据，格式如下：

```
STATE:状态值,ANGLE:角度值
```

**示例：**
```
STATE:0,ANGLE:245
STATE:1,ANGLE:412
STATE:2,ANGLE:780
```

**字段说明：**
- `STATE`：坐姿状态，取值 0、1、2
- `ANGLE`：角度传感器原始值，范围 0~1023

---

## 七、状态定义

| STATE 值 | 坐姿情况 | 硬件 LED 颜色 | 软件屏幕效果 |
|:--------:|---------|-------------|-------------|
| 0 | 端正 |  绿灯 | 屏幕正常 |
| 1 | 轻度不端正 |  蓝灯 | 屏幕轻度模糊 |
| 2 | 重度不端正 |  红灯 | 屏幕白屏 |

---

## 八、软件需要实现的功能

| 步骤 | 任务 |
|------|------|
| 1 | 打开串口（波特率 9600，根据实际 COM 口） |
| 2 | 按行读取数据 |
| 3 | 解析出 STATE 值 |
| 4 | 根据 STATE 控制屏幕效果 |

---

## 九、预计联调步骤

1. 硬件组烧录代码到 Arduino
2. 打开串口监视器，确认能看到数据输出
3. 软件组打开 C# 程序，确认能接收到数据
4. 手动转动角度传感器，观察屏幕效果变化
