<Window x:Class="SitRight.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        WindowState="Normal">

    <Grid x:Name="RootGrid" IsHitTestVisible="False">
        <!-- 模拟毛玻璃效果 -->
        <Grid x:Name="FrostedGrid" IsHitTestVisible="False">
            <!-- 半透明白色底色 -->
            <Rectangle>
                <Rectangle.Fill>
                    <SolidColorBrush Color="#F5FFFFFF" />
                </Rectangle.Fill>
            </Rectangle>

            <!-- 径向渐变：中心略浅，边缘略深 -->
            <Rectangle>
                <Rectangle.Fill>
                    <RadialGradientBrush Center="0.5,0.5" RadiusX="0.7" RadiusY="0.7">
                        <GradientStop Color="Transparent" Offset="0" />
                        <GradientStop Color="#20FFFFFF" Offset="0.7" />
                        <GradientStop Color="#30E0E8F0" Offset="1" />
                    </RadialGradientBrush>
                </Rectangle.Fill>
            </Rectangle>

            <!-- 噪点纹理模拟磨砂质感 -->
            <Rectangle Opacity="0.03">
                <Rectangle.Fill>
                    <DrawingBrush Viewport="0,0,60,60" ViewportUnits="Absolute" TileMode="Tile">
                        <DrawingBrush.Drawing>
                            <DrawingGroup>
                                <GeometryDrawing>
                                    <GeometryDrawing.Brush>
                                        <SolidColorBrush Color="White" />
                                    </GeometryDrawing.Brush>
                                    <GeometryDrawing.Geometry>
                                        <RectangleGeometry Rect="0,0,60,60" />
                                    </GeometryDrawing.Geometry>
                                </GeometryDrawing>
                            </DrawingGroup>
                        </DrawingBrush.Drawing>
                    </DrawingBrush>
                </Rectangle.Fill>
            </Rectangle>
        </Grid>
    </Grid>
</Window>
